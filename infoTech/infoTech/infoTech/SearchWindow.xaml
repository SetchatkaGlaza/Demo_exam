<Window x:Class="infoTech.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Поиск заявки по номеру" Height="400" Width="800" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Label Content="Номер заявки:" Margin="5"/>
            <TextBox x:Name="txtRequestNumber" Width="100" Margin="5"/>
            <Button x:Name="btnSearch" Content="Найти" Width="80" Margin="5" Click="btnSearch_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgResult" Grid.Row="2" AutoGenerateColumns="True" IsReadOnly="True" Margin="5"/>
    </Grid>
</Window>