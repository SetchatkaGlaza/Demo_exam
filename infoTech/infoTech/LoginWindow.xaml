<Window x:Class="infoTech.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="200" Width="300" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Логин:" Margin="5"/>
        <TextBox x:Name="txtLogin" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Пароль:" Margin="5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Button x:Name="btnLogin" Grid.Row="2" Grid.ColumnSpan="2" Content="Войти" Width="100" Height="25" Click="btnLogin_Click" Margin="5"/>
        <TextBlock x:Name="txtError" Grid.Row="3" Grid.ColumnSpan="2" Foreground="Red" TextWrapping="Wrap" Margin="5"/>
    </Grid>
</Window>